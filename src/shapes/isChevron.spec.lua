return function()
	-- local isChevron = require(script.Parent.isChevron)

	-- it("should detect if 3 points make up a chevron", function()
	-- 	local points = {
	-- 		Vector2.new(0, 0),
	-- 		Vector2.new(10, 10),
	-- 		Vector2.new(20, 0),
	-- 	}

	-- 	expect(isChevron(points)).to.equal(true)
	-- end)

	-- it("should detect if several points are a chevron", function()
	-- 	local points = {
	-- 		Vector2.new(0, 0),
	-- 		Vector2.new(5, 5),
	-- 		Vector2.new(10, 10),
	-- 		Vector2.new(15, 5),
	-- 		Vector2.new(20, 0),
	-- 	}

	-- 	expect(isChevron(points)).to.equal(true)
	-- end)

	-- it("should throw if given less than 3 points", function()
	-- 	local points = {
	-- 		Vector2.new(0, 0),
	-- 		Vector2.new(1, 1),
	-- 	}

	-- 	expect(function()
	-- 		isChevron(points)
	-- 	end).to.throw()
	-- end)
end
